enum Flavor { prod, dev }

enum LoggedInStatus { loggedOut, loggedIn }

enum ButtonType { text, normal, outline, icon }

enum AppStatus {
  initial,
  loading,
  success,
  complete,
  failure,
  thirdPartyFailure,
  resend,
  authenticated,
  logout,
  confirmation,
}

enum AppInitialization { disableAppRefetch }
